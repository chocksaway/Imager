CREATE TABLE images(
   id INT,
   name VARCHAR(50)
);


CREATE TABLE description(
     description_id    INT,
     name VARCHAR(50)
);

CREATE TABLE display(
    display_id        INT,
    height    INT,
    width     INT,
    border    boolean
);

